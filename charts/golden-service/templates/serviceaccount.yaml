{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "golden-service.serviceAccountName" . }}
  labels:
    {{- include "golden-service.labels" . | nindent 4 }}
{{- end }}
