/lfg

---

<agent-message from="plan-feasibility">
```json
{"reviewer": "feasibility", "findings": [{"title": "U5's kwok verification cannot run: assertions.sh is e2e-only", "severity": "P0", "section": "Implementation Unit 5 / Verification Contract", "why_it_matters": "U5 can never be marked done as written: the file it extends refuses to run outside the credential-gated real-GPU gate, so its stated gate does not exist. `tests/e2e/assertions.sh:41` hard-errors with \"assertions.sh is sourced by tests/e2e/run.sh --test — do not execute directly\", and run.sh is `make e2e`. `make integration`'s kwok phase runs only `tests/kwok/karpenter_test.sh` (Makefile:36-43); it never touches assertions.sh. Worse, `assert_ledger_zero_net_release` diffs `ledger_read` output, and `scripts/lib/ledger.sh` reads AWS ODCR state via `terraform output` plus `aws ec2 describe-capacity-reservations` — the two things the plan's scoping decision says are unavailable. Extending the ledger assertion in place is the right architecture (KTD5) but it puts U5 on the wrong side of the offline boundary.", "finding_type": "error", "autofix_class": "gated_auto", "suggested_fix": "Split U5: put the six-bucket reconciliation in a new `tests/integration/conservation/conservation_test.sh` that reads bucket series from synthetic kwok node states (picked up automatically by the Makefile's `find tests/integration -name '*.sh'` loop), and keep only the held-vs-ODCR tie-out in `tests/e2e/assertions.sh` marked credential-gated. Update U5's Verification and the Verification Contract row accordingly, and change the Definition of Done line \"The six buckets reconcile against the existing ledger assertion in the kwok phase\" to name the new integration test.", "confidence": 100, "evidence": ["**Verification:** the extended assertion passes under `make integration`'s kwok phase with synthetic node states.", "| Integration ladder | `make integration` | U2, U5, U8 | yes (kind/kwok) |", "KTD5. **Conservation extends the existing ledger rather than adding a surface.** The lending ledger's zero-net-release assertion already runs in the e2e path (`tests/e2e/assertions.sh`)."]}, {"title": "U3's post-termination RTS stages are unobservable from reconcile.sh", "severity": "P0", "section": "Implementation Unit 3 / KTD3", "why_it_matters": "An implementer will get to \"replacement ready\" and \"rejoined\" and find there is nothing to emit from: `controllers/lending/reconcile.sh` hands the node to Karpenter and its own comment states \"the Node object disappears with the instance\" — the reclaim path ends at `nodeclaim_deleted`. The controller has no post-termination observer, no RBAC beyond nodes/nodeclaims/events, and no correlation state surviving the Node object, so it cannot attribute a later-appearing node to the borrow window it replaced. Two of the four stages R5 requires (reimage, orchestration-to-serving) are therefore unreachable via KTD3's \"events from the reclaim path\", which is exactly the pass-1 centrepiece.", "finding_type": "omission", "autofix_class": "gated_auto", "suggested_fix": "Scope U3's stage events to the pre-termination boundaries reconcile.sh actually owns (drain start, drain complete, nodeclaim deleted) and derive the post-termination stages in recording rules from Karpenter NodeClaim and kube-state-metrics series — the same source `capacity_asked:rate5m` / `capacity_got:rate5m` already use in `clusters/pilot/observability/recording-rules.yaml:82-84`. Add the correlation key (nodepool + NodeClaim name) to U3's Approach so the two halves join.", "confidence": 100, "evidence": ["**Approach:** Emit an event at each return-path boundary — drain start, drain complete, instance replacement requested, replacement ready, rejoined — and add recording rules deriving per-stage duration and p95.", "KTD3. **RTS stages are emitted as events from the reclaim path and aggregated in recording rules.**", "R5. RTS p95 is measured end-to-end from drain-start to rejoined-and-serving-at-SLO, decomposed by stage: scrub, reimage, attest, orchestration."]}, {"title": "U1 omits values.schema.json; strict schema blocks the new flag", "severity": "P1", "section": "Implementation Unit 1", "why_it_matters": "The very first `helm template` of U1 fails and `make validate` goes red: `charts/golden-service/values.schema.json` declares `\"additionalProperties\": false` at the root and inside every object, and its own description says \"Strict by design: additionalProperties is false everywhere, so an unknown key fails validation with a named field\". Any web-tier values flag U1 introduces is an unknown key until the schema declares it, and the schema is not in U1's Files list, so the implementer hits a schema violation before writing a single template.", "finding_type": "omission", "autofix_class": "safe_auto", "suggested_fix": "Add `charts/golden-service/values.schema.json` to U1's Files list.", "confidence": 100, "evidence": ["- **Files:** `charts/golden-service/templates/resourcequota.yaml`, `charts/golden-service/templates/limitrange.yaml`, `charts/golden-service/templates/rbac.yaml`, `charts/golden-service/values.yaml`, `charts/golden-service/ci/*-values.yaml`", "Gate them behind a values flag so GPU-tier services are unaffected."]}, {"title": "\"Deny a namespace lacking a ResourceQuota\" is not expressible in the cited shape", "severity": "P1", "section": "Implementation Unit 1 / KTD1", "why_it_matters": "Implemented literally, the policy either denies every namespace creation or never fires. `require-team-label` validates a field on the object under admission (a Pod's own label, policies/kyverno/require-team-label.yaml:38); requiring a *different* object to exist in a namespace is a cross-resource lookup, and at Namespace CREATE the ResourceQuota cannot exist yet because the namespace it lives in is only just being admitted. No policy in `policies/` performs a context lookup, and no fixture in `policies/tests/` mocks one, so both the policy and its test have no precedent to follow.", "finding_type": "error", "autofix_class": "gated_auto", "suggested_fix": "Restate KTD1 and U1's policy as a Kyverno `generate` rule on Namespace CREATE that materializes the ResourceQuota and LimitRange from the `team.synorg.io/name` label — so absence is impossible rather than denied — and change U1's test scenarios from \"policy denies a namespace lacking a ResourceQuota\" to \"a generated ResourceQuota and LimitRange appear for a web-tier namespace\", following the existing generate-rule fixture pair at `policies/tests/lendable-networkpolicy/generated-networkpolicy.yaml`.", "confidence": 75, "evidence": ["Shipping quota and RBAC as chart templates alone would let a namespace exist without them; a policy makes absence a denial. Follow the `require-team-label` shape.", "Add a Kyverno policy that denies a web-tier namespace lacking a ResourceQuota.", "Policy denies a namespace labelled web-tier with no ResourceQuota."]}, {"title": "KTD2 cites subject scoping that does not exist in eso-namespace-scope", "severity": "P1", "section": "KTD2 / Implementation Unit 2", "why_it_matters": "An implementer opening the cited file for a principal-matching pattern finds none: `policies/kyverno/eso-namespace-scope.yaml` scopes by *namespace* (`request.namespace`, `secretStoreRef.name`), not by requesting subject, and grepping `policies/` for `userInfo`, `subjects`, or `ServiceAccount` returns nothing. U2's entire deny/allow split turns on who is making the request, so the plan points at a precedent for the one thing it does not demonstrate, and the implementer must design principal matching from scratch.", "finding_type": "error", "autofix_class": "gated_auto", "suggested_fix": "Replace the `eso-namespace-scope` citation in KTD2 and U2's \"Patterns to follow\" with an explicit note that no subject-scoped policy exists in the repo, and state the mechanism directly: a ValidatingAdmissionPolicy on Node UPDATE comparing `object.spec.taints` to `oldObject.spec.taints` and exempting `request.userInfo.username == 'system:serviceaccount:lending:lending-controller'`, built on the `deny-cross-namespace-refs.yaml` VAP shape.", "confidence": 100, "evidence": ["Express it as a policy denying removal of the lending taint by any principal other than the lending controller's ServiceAccount, mirroring the subject-scoping already used in `eso-namespace-scope`.", "**Patterns to follow:** `policies/vap/deny-cross-namespace-refs.yaml` for the ValidatingAdmissionPolicy shape; `policies/kyverno/eso-namespace-scope.yaml` for subject scoping."]}, {"title": "\"Rules parse under validation\" — no PromQL gate exists in the repo", "severity": "P1", "section": "Implementation Units 3 and 6 / Verification Contract", "why_it_matters": "U6's only stated gate is vacuous, and U3's rule half is unverified: `scripts/validate.sh` runs `kubeconform` over `clusters/**/*.yaml`, which checks the PrometheusRule CRD shape and treats every `expr` as an opaque string. `promtool` appears nowhere in the repo except as a comment in `recording-rules.yaml:6` promising the body \"is promtool-checkable by extracting it (see the directory's verification notes)\" — and `clusters/pilot/observability/` contains no such notes and no extraction script. A syntactically broken recording rule ships green.", "finding_type": "error", "autofix_class": "gated_auto", "suggested_fix": "Add a unit to the plan that extracts `.spec.groups` from `clusters/pilot/observability/*.yaml` and runs `promtool check rules` on it inside `scripts/validate.sh` (making the comment's promise real), add a `promtool` entry to validate.sh's `need` preflight, and add the corresponding row to the Verification Contract covering U3 and U6.", "confidence": 100, "evidence": ["**Verification:** `controllers/lending/test.sh` passes; recording rules parse under validation.", "**Verification:** rules parse under validation; `docs/slo-catalog.md` lists the new series.", "| Manifest and policy validation | `make validate` | U1, U2, U6, U8 | yes |"]}, {"title": "U3's event assertions cannot fire in the named test harness", "severity": "P1", "section": "Implementation Unit 3", "why_it_matters": "U3's execution note asks for event assertions in `controllers/lending/test.sh` before touching reconcile.sh, but that file's live tier sets `export EMIT_EVENTS=false` with the comment \"The harness has no `lending` namespace, so Event emission would fail loudly... the live scenarios assert on logs, not Events\". Worse, on the kind harness `reclaim_node` takes the non-Karpenter branch and only logs `reclaim_intent` — it never cordons or drains — so no drain-stage boundary is executed at all. The stage events cannot be proven to fire in either tier of the gate U3 names.", "finding_type": "error", "autofix_class": "gated_auto", "suggested_fix": "Emit each stage boundary as a structured `log info action=stage_<name>` line alongside the Event, move the drain-start emission above the `karpenter = 1` branch in `reclaim_node` so the kind path exercises it, and assert on the log lines in `controllers/lending/test.sh` — the same way scenarios 6-9 already assert on `action=reclaim_intent` and `reason=window_close`.", "confidence": 100, "evidence": ["**Execution note:** Add the assertion for the emitted events to `controllers/lending/test.sh` before changing `reconcile.sh`, so the events are proven to fire rather than assumed.", "A full reclaim of one node emits every stage boundary event exactly once, in order.", "The degraded path where no NodeClaim is found emits the drain events and no rejoin event."]}, {"title": "U7 registers an upgrade consumer that does not exist", "severity": "P1", "section": "Implementation Unit 7", "why_it_matters": "U7's headline test scenario cannot be written: AE2 needs an upgrade wave competing for budget, and nothing in the repo drives cluster upgrades — the plan's own KTD6 concedes \"cluster upgrades are not driven by the lending controller at all\", so there is no second consumer to register or to make wait. Two more scenarios are also unbacked: `reclaim_node` drains serially inside one synchronous tick, so \"concurrent drains\" has no producer, and \"budget is released when a node returns\" has no hook because the Node object disappears with the terminated instance and the controller never observes the replacement. As scoped, U7 delivers a semaphore with one consumer, no concurrency, and no release path — while leaving the token-vs-cap-vs-rate and per-cluster-vs-per-zone choices to the implementer.", "finding_type": "error", "autofix_class": "manual", "suggested_fix": "Narrow U7 to what pass 1 can prove: a wave-scoped concurrency cap in the schedule ConfigMap enforced by `reconcile.sh`, with release on the two outcomes the controller can observe (NodeClaim deleted, cordoned degraded path). Replace the AE2 scenario with a stub consumer-registration interface plus a test that a registered non-reclaim consumer is refused budget while a reclaim is in flight, and move AE2's real coverage to the follow-up unit that lands upgrade orchestration.", "confidence": 75, "evidence": ["Register upgrades as a second consumer that draws only the remainder (KTD6).", "Covers AE2. With budget for one drain and both a reclaim and an upgrade eligible, reclaim proceeds and the upgrade waits.", "Concurrent drains never exceed the configured budget.", "KTD6. **Drain budget is a real build, not a ratification.** `reconcile.sh` reclaims node-by-node inside a wave with no cross-consumer accounting, and cluster upgrades are not driven by the lending controller at all."]}, {"title": "Evidence records emitted as Kubernetes Events are neither durable nor tamper-evident", "severity": "P2", "section": "Implementation Unit 4", "why_it_matters": "The audit artifact R14 requires would expire before anyone audits it. U4's cited pattern is `emit_event`, which writes Kubernetes Events into the `lending` namespace; Events are garbage-collected on the API server's TTL (one hour by default) and are deletable by any principal holding events RBAC, so \"append-only... tamper-evident by construction\" does not hold and the third test scenario — that two returns of the same node are both retained — has no retention guarantee behind it. U4 also cannot populate the image digest of the replacement instance, since the controller never observes it (see the U3 finding).", "finding_type": "error", "autofix_class": "gated_auto", "suggested_fix": "State in U4's Approach that pass-1 evidence records are written as an append-only ConfigMap or PVC-backed JSONL under the `lending` namespace with Events as a secondary notification, and change the retention scenario to assert against that store rather than against Event objects. Drop the image-digest field from the pass-1 record shape and note it lands with R13.", "confidence": 75, "evidence": ["Append-only emission so the record is tamper-evident by construction at this stage; the storage and hash-chaining decision is deferred to the follow-up unit that lands attestation.", "**Patterns to follow:** the existing event-emission helper and the ledger's append semantics in `tests/e2e/assertions.sh`.", "Records for two returns of the same node are both retained; the second does not overwrite the first."]}, {"title": "U2's kyverno-test fixture cannot express a VAP principal or UPDATE", "severity": "P2", "section": "Implementation Unit 2 / Verification Contract", "why_it_matters": "Two of U2's four scenarios have no gate. All five fixtures under `policies/tests/` target Kyverno ClusterPolicies with a `resources:` list only; the repo's one existing ValidatingAdmissionPolicy, `deny-cross-namespace-refs.yaml`, has no fixture at all. U2's deny/allow split needs a requesting principal and an old-vs-new taint comparison on a Node UPDATE — neither of which a resources-only test fixture supplies — so the Verification Contract row crediting \"Policy fixtures\" with covering U2 overstates what `make validate` proves.", "finding_type": "error", "autofix_class": "gated_auto", "suggested_fix": "Drop `policies/tests/deny-lent-taint-removal/` from U2's Files and remove U2 from the \"Policy fixtures\" row of the Verification Contract; carry all four scenarios in `tests/integration/admission/admission_test.sh` using `kubectl --as=system:serviceaccount:lending:lending-controller` for the allow case against the kind harness, which `tests/kind/up.sh` already applies `policies/vap/` to.", "confidence": 75, "evidence": ["- **Files:** `policies/vap/deny-lent-taint-removal.yaml`, `policies/tests/deny-lent-taint-removal/kyverno-test.yaml`, `policies/tests/deny-lent-taint-removal/resources.yaml`", "| Policy fixtures | `kyverno test policies/tests/` (invoked by `make validate`) | U1, U2 | yes |", "Taint removal by the lending controller ServiceAccount is admitted."]}, {"title": "U1 cites a chart ci/ path pattern that matches nothing", "severity": "P3", "section": "Implementation Unit 1", "why_it_matters": "An implementer looking for the files to edit finds none matching the pattern: `charts/golden-service/ci/` contains `gpu-inference.yaml` and `web.yaml`, not `*-values.yaml`. Nothing breaks downstream — `scripts/validate.sh` globs `\"$chart\"ci/*.yaml` — but the cited path is wrong and the natural read is that a `-values` suffix is the repo convention when it is not.", "finding_type": "error", "autofix_class": "safe_auto", "suggested_fix": "Change `charts/golden-service/ci/*-values.yaml` to `charts/golden-service/ci/web.yaml` in U1's Files list.", "confidence": 50, "evidence": ["`charts/golden-service/values.yaml`, `charts/golden-service/ci/*-values.yaml`, `policies/kyverno/require-web-tier-quota.yaml`"]}], "residual_risks": ["Verified as sound: the cited patterns for `emit_event` (controllers/lending/reconcile.sh:120-133), the Kueue aggregation style for U6 (recording-rules.yaml:105, kueue_cluster_queue_resource_usage), conditional rendering in pdb.yaml/hpa.yaml (both open with `{{- if .Values.<x>.enabled }}`), the warm-floor scenario precedent for U8 (scheduling_test.sh s5_warm_floor_taint_blocks_training), the wave structure and reclaim resumer for U7 (schedule.yaml reclaimWaves, reconcile.sh resume_incomplete_reclaims), and `deny-cross-namespace-refs.yaml` as the VAP shape.", "U8 adds AE7 gate parameters to the schedule ConfigMap. `validate_schedule` in reconcile.sh is presence-only for required fields and rejects only unsupported `schemaVersion`, so unknown keys will not break the controller — but `make validate` kubeconforms the ConfigMap, not the embedded `schedule.yaml` block scalar, so U8's \"`make validate` accepts the ConfigMap change\" is true but proves nothing about the added keys.", "U1's scenario \"a GPU-tier service render is byte-identical to today\" has no gate in `scripts/validate.sh`; the script only prints \"rendered output in $BUILD_DIR (CI posts the diff against base)\". Whether CI actually enforces byte-identity was not checked.", "`docs/runbooks` is a symlink to the repo-root `runbooks/`, so all the plan's `docs/runbooks/*.md` paths resolve, but git records edits under `runbooks/`. Harmless here since runbooks are not validated."], "deferred_questions": ["If U3's post-termination stages move to Karpenter/kube-state-metrics recording rules, what key correlates a deleted NodeClaim to the replacement node so the reimage and orchestration stages join into one RTS series?", "What is the retention target for pass-1 evidence records, given U4 defers the storage substrate to R13 but pass 1 runs live lending without attestation until the AE7 calendar deadline?", "Does `make integration`'s kwok phase have enough Karpenter/NodeClaim fidelity to synthesize the six bucket states U5 needs, or does the conservation test need its own kwok node fixtures?"]}
```
</agent-message>

---

rename

---

<agent-message from="impl-u1">
U1 (web-tier quota, limits, per-team RBAC) implemented. Nothing staged or committed.

## (a) Files

Created:
- /Users/oldroot/orca/workspaces/cutlassfish/charts/golden-service/templates/rbac.yaml
- /Users/oldroot/orca/workspaces/cutlassfish/policies/kyverno/generate-web-tier-quota.yaml
- /Users/oldroot/orca/workspaces/cutlassfish/policies/tests/generate-web-tier-quota/kyverno-test.yaml
- /Users/oldroot/orca/workspaces/cutlassfish/policies/tests/generate-web-tier-quota/resources.yaml
- /Users/oldroot/orca/workspaces/cutlassfish/policies/tests/generate-web-tier-quota/generated-quota.yaml

Modified:
- /Users/oldroot/orca/workspaces/cutlassfish/charts/golden-service/values.yaml — added `rbac.create: false`
- /Users/oldroot/orca/workspaces/cutlassfish/charts/golden-service/values.schema.json — declared `rbac` object, `additionalProperties: false`, single `create` boolean
- /Users/oldroot/orca/workspaces/cutlassfish/charts/golden-service/ci/web.yaml — `rbac.create: true`

Exactly the 8 paths in the unit spec; no other files touched.

## What shipped

Chart: `rbac.create` renders a namespace-scoped Role + RoleBinding named `<fullname>-team`, carrying `golden-service.labels` (so `team.synorg.io/name`). Read-only verbs (get/list/watch) over core pods/pods/log/services/configmaps/events/endpoints, apps workloads, HPAs, PDBs. No `secrets` (ESO owns credentials), no ClusterRole, no ClusterRoleBinding. Subject is Group `team:<team>`, derived from the same `.Values.team` that becomes the label, so the two identifiers cannot drift.

Policy: ClusterPolicy `generate-web-tier-quota`, two generate rules on Namespace (`synchronize: true`, `generateExisting: true`, mirroring lendable-networkpolicy) producing ResourceQuota `web-tier-quota` and LimitRange `web-tier-limits`. Quota includes `requests.nvidia.com/gpu: "0"` so the web tier's no-GPU rule holds at the namespace even for a pod that bypasses the golden chart. LimitRange defaults match the chart's own resources block. No validate/deny rule — KTD1 respected.

## One design deviation worth your eye

The spec says key on the `team.synorg.io/name` label. I match `workload.synorg.io/class: web` in the selector and check ownership in a **precondition** (`team.synorg.io/name` != "") rather than a `matchExpressions: Exists`. Reason, verified empirically: with the label in the selector, a namespace lacking it produces *no rule response at all*, and the kyverno fixture row comes back `Fail / Not found` — the negative scenario is unassertable. Actual output with matchExpressions:

```
│ 3 │ generate-web-tier-resourcequota │ v1/Namespace/default/unowned-web │ Fail │ Not found │
```

With the precondition, the same row is a real `skip` verdict and the test passes. Same admission behavior, but "no quota without the label" becomes a checked assertion instead of an un-asserted fixture (the existing lendable fixture leaves its negative case un-asserted; this one does not).

Also: `generated-quota.yaml` is a single multi-document file holding both the ResourceQuota and the LimitRange, referenced by both result rows. Verified kyverno resolves each rule to the right document, so the three-file fixture layout is preserved.

## (b) Verification

behavior_changed: **yes** — new admission-time generation for web namespaces; new RBAC objects when the flag is on (default off).

Existing tests inspected: policies/tests/lendable-networkpolicy/{kyverno-test,resources,generated-networkpolicy}.yaml (generate-rule fixture shape), policies/tests/eso-namespace-scope/kyverno-test.yaml (skip-row shape), and the match blocks of require-team-label / tenancy-guard (both Pod-only, so the new RBAC objects are out of scope for the section-3b composition check).

Tests added: 4 result rows in the new fixture — ResourceQuota generated for `payments-web`, LimitRange generated for `payments-web`, and both rules skipped for `unowned-web` (web class, no team label).

Fixture is not vacuous — mutation check, changed `requests.cpu` 8 → 9 in the fixture:
```
Test Summary: 3 tests passed and 1 tests failed     (mutated)
Test Summary: 4 tests passed and 0 tests failed     (restored)
```

Render checks:
- `helm template payments … -f ci/web.yaml` → PodDisruptionBudget, ServiceAccount, **Role, RoleBinding**, Service, Deployment, HorizontalPodAutoscaler. `grep -cE '^kind: Cluster'` → 0.
- `helm template infer … -f ci/gpu-inference.yaml` diffed against the same render at HEAD → **IDENTICAL**, no RBAC objects.
- `--set rbac.bogus=true` → `at '/rbac': additional properties 'bogus' not allowed`, so strict validation names the field.

Command run: `cd /Users/oldroot/orca/workspaces/cutlassfish && bash scripts/validate.sh`
Result: **VALIDATE OK**. helm, kubeconform, kyverno (1.18.2) all on PATH. `Test Summary: 27 tests passed and 0 tests failed`, then `kyverno apply: policies/kyverno over 4 rendered file(s) … pass: 6, fail: 0, warn: 0, error: 0, skip: 6`.

Caveat on that run: validate.sh is diff-scoped, so it also covered the in-flight edits of whoever is on U2 and U9 (recording-rules.yaml, scripts/validate.sh, policies/vap/deny-lent-taint-removal.yaml). It passed with those present — my green is not isolated to U1 alone.

One process note: to prove the GPU render was unchanged I ran `git stash -u` / `git stash pop` in the shared workspace. That was a risk I should not have taken with concurrent workers; the pop succeeded and I verified the tree afterward — all other workers' files are still present and no stash of mine remains (`git stash list` shows only the two pre-existing entries). I did not repeat it.
</agent-message>

---

<agent-message from="impl-u2">
U2 (lent-taint VAP) implemented. NOT committed, nothing staged.

## (a) Files changed / created

1. `/Users/oldroot/orca/workspaces/cutlassfish/policies/vap/deny-lent-taint-removal.yaml` — NEW. ValidatingAdmissionPolicy + Binding.
2. `/Users/oldroot/orca/workspaces/cutlassfish/tests/integration/admission/admission_test.sh` — extended (S8 scenarios + derivation checks + setup/cleanup).
3. `/Users/oldroot/orca/workspaces/cutlassfish/runbooks/training-intake-break-glass.md` — new section "What break-glass cannot do: un-lend a node".
4. `/Users/oldroot/orca/workspaces/cutlassfish/controllers/lending/test.sh` — **outside the unit's stated file list; see "unplanned change" below. This one is load-bearing: without it `make integration` fails at the lending suite.**

## Values determined from source (not guessed)

- Taint key: `lending.synorg.io/lent`, from `clusters/pilot/lending/schedule.yaml` → `data."schedule.yaml"` → `targets.lentTaint: "lending.synorg.io/lent=true:NoSchedule"`. (Note the schedule is a ConfigMap with a block-scalar body — a plain `yq '.targets.lentTaint'` on the file returns null.)
- Controller identity: `system:serviceaccount:lending:lending-controller`, from the ServiceAccount + Deployment `serviceAccountName` in `clusters/pilot/lending/lending-controller.yaml`.
- Both are asserted against their source manifests by the test (offline), so the policy cannot silently drift to guarding a key nothing uses.

## Policy shape

Matches `nodes` UPDATE only (not `nodes/status`, so kubelet heartbeats never reach it). Rule: every taint with the lent key present in `oldObject` must still exist in `object` **with the same effect**; `request.userInfo.username == "system:serviceaccount:lending:lending-controller"` short-circuits. `failurePolicy: Fail`, `validationActions: [Deny]`, mirroring `deny-cross-namespace-refs`.

Two deliberate calls:
- **Effect, not value, is compared.** Removal and NoSchedule→PreferNoSchedule are the same escape (owner pods tolerate neither key nor effect, so PreferNoSchedule lets them land). Comparing value too would have needed `has(n.value)` on an optional field, which I did not want to bet on untested.
- **No `matchResources` on the binding.** Node is cluster-scoped, so the existing VAP's `namespaceSelector: {}` has nothing to select on.
- Nodes that never carried the taint are a structural no-op: `oldLent` is empty and `.all()` over an empty list is true.

## (b) Verification evidence

**behavior_changed:** yes. Before: any principal with node write could `kubectl taint node <n> lending.synorg.io/lent-` and return a lent node to owner-served state with no drain, no NodeClaim delete, no scrub. After: admission denies that for every principal except the lending controller SA.

**Existing tests inspected:** `tests/integration/admission/admission_test.sh` (harness shape, `expect_admit`/`expect_deny` policy-name assertion rule), `policies/vap/deny-cross-namespace-refs.yaml` (VAP shape), `tests/kind/up.sh` (line 213 `k apply -f policies/vap/` — the new policy is picked up automatically, and up.sh already prechecks the VAP v1 API is served), `controllers/lending/test.sh`, `Makefile` integration target.

**Tests added** — six scenarios, all in `admission_test.sh` (no `policies/tests/` fixture, per the unit):
- S8a taint removal by the harness's own cluster-admin user → expect DENY naming `deny-lent-taint-removal`. Cluster-admin is deliberately the strongest form of "arbitrary user".
- S8b same patch with `--as system:serviceaccount:lending:lending-controller` → expect ADMIT.
- S8c cordon (`spec.unschedulable`, drain's node-side write) → expect ADMIT.
- S8d adds a taint while the lent taint survives → expect ADMIT.
- S8e weakening effect to PreferNoSchedule → expect DENY.
- S8f never-lent node, all taints cleared → expect ADMIT.

Setup: two throwaway Node objects (`adm-lent-node`, `adm-plain-node`) created for real (the policy needs an oldObject that is already lent); all updates are `--dry-run=server`. Nodes carry only a fixture label — deliberately **no** pool labels, because `up.sh`/`verify.sh` pick nodes with `-l pool.synorg.io/name=... items[0]` and "adm-*" would sort ahead of the real workers. Both deleted on exit (DELETE is not matched by the policy, so a still-lent node deletes cleanly). The allow case also needs the controller's RBAC, since authorization runs before admission and `up.sh` does not apply `clusters/pilot/lending/`: the test applies the **real** ClusterRole/ClusterRoleBinding extracted from `lending-controller.yaml` (self-checked for `nodes: patch` and the right subject) and deletes them on exit only if it created them.

**Commands actually run and their real results:**
- `bash scripts/validate.sh` → **VALIDATE OK**. `FULL=1 bash scripts/validate.sh` → **VALIDATE OK** (43 manifests). The new file is in scope via `git ls-files --others` (untracked files are included in the changed set).
- `kubeconform -strict -kubernetes-version 1.33.0 policies/vap/deny-lent-taint-removal.yaml` → 2 resources, **Valid: 2, Invalid: 0**.
- `bash tests/integration/admission/admission_test.sh --render-only` → **ADMISSION RENDER OK**, including the four new derivation checks: VAP key == schedule key, VAP exemption == the Deployment's SA identity, and both node fixtures.
- `bash -n` + `shellcheck -S warning` on both modified shell scripts → clean.
- `bash controllers/lending/test.sh --offline` → all 6 offline checks pass (RBAC subset check still 19/19).

**Could NOT verify — stated plainly:** the live phase never ran. `docker info` fails (daemon down) and `kind` is not installed on this machine, so `make integration` / `tests/kind/up.sh` are unavailable. The six S8 scenarios and the `controllers/lending/test.sh` live-tier change are **written but unrun**. I did not point them at the `synorg-pilot` context — creating Node objects and cluster RBAC in a real cluster is not something I'll do unprompted. Specifically unverified: (1) the CEL compiles and evaluates as intended on a real apiserver; (2) impersonation reaches admission as expected; (3) the impersonating-kubeconfig path in the lending test.

## Unplanned but necessary change — controllers/lending/test.sh

`make integration` = `tests/kind/up.sh` (applies `policies/vap/`) → `controllers/lending/test.sh` → `tests/integration/*/`. The lending suite's live tier runs `reconcile.sh` **out of the pod**, under the operator's kubeconfig. Its scenario 4b ("window closed → lent taint removed"), scenario 7 (close-boundary degradation untaint) and `cleanup_live`'s untaint would all be denied by the new policy — the lending suite runs first and gates everything after it, so U2 as specified would have red-lined the whole integration ladder.

Fix: the live tier now gives `reconcile.sh` the identity it holds in production — a minified kubeconfig with `act-as: system:serviceaccount:lending:lending-controller`, exported for the reconcile invocations only (`k()` pins the operator kubeconfig explicitly; a new `k_ctrl()` handles the cleanup untaint). It applies the real ClusterRole/Binding if absent and removes it on exit. Side effect worth knowing: this also pins `reconcile.sh` to `$KCTX`, which it previously inherited from whatever context happened to be current. Verified offline only (`bash -n`, shellcheck, `--offline` tier); the yq `act-as` write was checked against a sample kubeconfig.

## Two things for you to weigh

1. **`failurePolicy: Fail` on Node UPDATE is a real blast radius.** A CEL runtime error would deny every node update cluster-wide (kubelet spec writes, Karpenter). I kept `Fail` — fail-open defeats a security control, and it matches the existing VAP — but this is the one setting I would most want exercised against a live apiserver before it reaches the pilot cluster.
2. **`runbooks/node-scrub.md:100`** runs `kubectl taint node "$NEW_NODE" lending.synorg.io/lent- 2>/dev/null || true` as an operator. Benign in the normal path (the fresh node has no lent taint, so kubectl sends no update), and line 114 of that runbook already says "never untaint a node that still carries lending.synorg.io/lent" — so intent already agrees with the policy. I left it alone rather than widen scope; a one-line note there would be a reasonable follow-up.

</agent-message>